/public/
/resources/
.hugo_build.lock
.DS_Store
__pycache__/
*.pyc

# Perkakas test (Vitest) — dev-only, tidak dipakai saat build Hugo
/node_modules/

# Panduan internal Claude Code — jangan publikasikan ke repo publik
/CLAUDE.md
