/public/
/resources/
.hugo_build.lock
.DS_Store
__pycache__/
*.pyc

# Perkakas test (Vitest) — dev-only, tidak dipakai saat build Hugo
/node_modules/

# Panduan internal Claude Code — jangan publikasikan ke repo publik
/CLAUDE.md

# Ringkasan sementara pratinjau medsos (dibuat di runner, jangan di-commit)
preview-summary.json

# Rahasia — repo ini PUBLIK, dan alur kerjanya menyentuh token GitHub,
# kunci Gemini, serta kunci Pexels. Satu .env yang ter-commit langsung
# terbaca seluruh internet. Per 19 Agu 2026 belum pernah ada .env di sini;
# ini PAGAR, bukan tambalan. Rahasia asli hidup di GitHub Secrets.
.env
.env.*
!.env.example
